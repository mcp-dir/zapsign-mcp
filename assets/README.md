# Assets

- `logo.svg` — logotipo do ZapSign (fonte: `mcp_registry.logo_url`). Substituir pelo definitivo antes do launch nas redes / Smithery.

## Pendente antes do launch

- `logo.png` 512×512 (alguns registries exigem PNG)
- `screenshots/` — 2–4 capturas de uso (dados sintéticos / anonimizados)
- `demo.gif` — fluxo instalar → usar

> ⚠️ Nunca commitar screenshots com dados reais. Use sandbox / dados sintéticos. Raw fica em `assets/screenshots-raw/` (gitignored).
